def query(query_string, *params):
    print(f"Executing query: {query_string} with params: {params}")
    return 0  # Simulating no duplicates in database

def execute(query_string, *params):
    print(f"Executing query: {query_string} with params: {params}")